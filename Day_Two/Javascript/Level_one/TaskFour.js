const input = require("prompt-sync")();

const first = Number(input("Enter first number => "))

const second = Number(input("Enter second number => "))

let total = first * second

console.log(`${first} x ${second} = ${total}`)


